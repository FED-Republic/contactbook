import { createStore } from 'vuex'
import initialConfig from './initialConfig'

export default createStore({
  state: {
  },
  mutations: {
  },
  actions: {
  },
  modules: {
      initialConfig
  }
})
